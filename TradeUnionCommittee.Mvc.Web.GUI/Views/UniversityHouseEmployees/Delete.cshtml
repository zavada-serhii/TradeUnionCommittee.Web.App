@model TradeUnionCommittee.BLL.DTO.UniversityHouseEmployeesDTO
@{
    ViewData["Title"] = "Delete";
}

<section class="add-user container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-12 add-user_block">

            <div class="row align-items-center mt-3">
                <div class="col-md-10">
                    <h1>Видалення приватного житла</h1>
                    <h3 class="text-muted">Ви впевнені, що хочете приватне житло?</h3>
                </div>
            </div>
            <hr>
            @if (TempData["ErrorsList"] == null)
            {
                <form asp-action="Delete">

                    <input type="hidden" asp-for="HashId" />
                    <input type="hidden" asp-for="HashIdEmployee" />

                    <dl class="row">
                        <dt class="col-md-3">
                            <a>Місто</a>
                        </dt>
                        <dd class="col-md-5">
                            <p class="lead" style="font-size: 15px;">@Model.City</p>
                        </dd>
                    </dl>

                    <dl class="row">
                        <dt class="col-md-3">
                            <a>Вулиця</a>
                        </dt>
                        <dd class="col-md-5">
                            <p class="lead" style="font-size: 15px;">@Model.Street</p>
                        </dd>
                    </dl>

                    <dl class="row">
                        <dt class="col-md-3">
                            <a>Номер дому</a>
                        </dt>
                        <dd class="col-md-5">
                            <p class="lead" style="font-size: 15px;">@Model.NumberHouse</p>
                        </dd>
                    </dl>

                    <dl class="row">
                        <dt class="col-md-3">
                            <a>Номер квартири</a>
                        </dt>
                        <dd class="col-md-5">
                            <p class="lead" style="font-size: 15px;">@Model.NumberApartment</p>
                        </dd>
                    </dl>

                    <dl class="row">
                        <dt class="col-md-3">
                            <a>Дата розподілу</a>
                        </dt>
                        <dd class="col-md-5">
                            <p class="lead" style="font-size: 15px;">@Model.DateReceiving.Value.ToString("dd.MM.yyyy")</p>
                        </dd>
                    </dl>

                    <div class="row justify-content-between">
                        <div class="form-group col-lg-3">
                            <div>
                                <a asp-action="Index" asp-route-id="@Model.HashIdEmployee">Повернутися</a>
                            </div>
                        </div>
                        <div class="col-md-6 text-right">
                            <input type="submit" value="Видалити" class="btn btn-danger" />
                        </div>
                    </div>

                </form>
            }
            else
            {
                await Html.RenderPartialAsync("_Errors", TempData["ErrorsList"]);
            }
        </div>
    </div>
</section>