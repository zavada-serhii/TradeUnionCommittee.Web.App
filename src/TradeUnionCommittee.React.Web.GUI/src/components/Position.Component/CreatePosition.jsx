import React from 'react';
import { withRouter } from "react-router-dom";

class CreatePosition extends React.Component {

    render() {
        return (
            <div>
                Create postion component
            </div>
        );
    }
}

export default withRouter(CreatePosition)
